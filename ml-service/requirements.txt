# ShoeAR recommender ML service (Flask + scikit-surprise + scikit-learn).
# scikit-surprise needs numpy<2 and a C build toolchain — on Windows install
# via a prebuilt wheel if pip build fails (see README).
Flask==3.0.3
waitress==3.0.0
PyMySQL==1.1.1
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
scikit-surprise==1.1.4
# AR auto-fit (autofit.py): loads/normalises/exports .glb shoe models.
# Uses the numpy + scipy already listed above (scipy powers mesh.split()).
trimesh==4.5.3
# Quadric edge-collapse backend for mesh decimation (high-poly -> mobile budget).
# Optional: autofit falls back to no decimation if it isn't installed.
fast-simplification==0.1.13
