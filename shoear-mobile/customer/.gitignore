# Flutter / Dart build + tooling artefacts (regenerated locally)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
.pub-cache/
.pub/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/

# Generated platform folders are created locally with `flutter create .`
# (kept out of git so the repo stays lean — see README).
/android/
/ios/
/linux/
/macos/
/windows/
/web/

# Local run scripts holding the Google Places API key (never commit the key).
# Copy run.sh.example -> run.sh (Linux/Mac) or run.bat.example -> run.bat
# (Windows) and paste your key into the copy.
run.sh
run.bat
run.ps1

# Misc
*.log
.DS_Store
